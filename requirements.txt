# Core (assessment-permitted) libraries
numpy==2.2.6
pandas==2.2.3
scikit-learn==1.6.1
matplotlib==3.10.6
# Notebook infrastructure (create / execute / export)
jupyter-core==5.8.1
ipykernel==6.30.1
nbformat==5.10.4
nbclient==0.10.2
nbconvert==7.16.6
# Data acquisition only (Kaggle download + parquet extract; not used for modelling)
kagglehub==1.0.2
pyarrow==21.0.0
# Data acquisition only (reads the BLS Excel file)
openpyxl==3.1.5
